
# 게시글 테이블 

CREATE TABLE BASIC_BOARD (
	BOARD_NUM INT PRIMARY KEY AUTO_INCREMENT,
	TITLE VARCHAR(30),
	WRTIER VARCHAR(20),
	CONENTE VARCHAR(50),
	READ_CNT INT  DEFAULT 0,
	CREATE_DATE DATETIME DEFAULT SYSDATE()

);

INSERT INTO BASIC_board (TITLE,WRTIER,CONENTE) VALUES('제목1','작성자1','내용1');
INSERT INTO BASIC_board (TITLE,WRTIER,CONENTE,READ_CNT,CREATE_DATE) VALUES('제목2','작성자2','내용2',5,'2025-02-04');

SELECT * FROM basic_board;

COMMIT;

SELECT * FROM basic_board ;

SELECT READ_CNT FROM basic_board;

SELECT * FROM BASIC_BOARD WHERE BOARD_NUM = 1;

SELECT * FROM emp WHERE



















