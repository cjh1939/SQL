

# 회원 테이블
CREATE TABLE REACT_MEMBER(
	MEM_ID VARCHAR(10)PRIMARY KEY
	, MEM_NAME VARCHAR(10) NOT NULL
	,MEM_PW VARCHAR(10) NOT NULL
	,MEM_TEL VARCHAR(15) UNIQUE 
	,MEM_INTRO VARCHAR(30)
	,JOIN_DATE DATETIME DEFAULT SYSDATE()

);


INSERT INTO REACT_MEMBER VALUES('kim','이순신','1234','010-1111-2222','안녕하세요',NOW());

COMMIT;


SELECT MEM_ID,MEM_NAME,MEM_TEL,JOIN_DATE
        FROM REACT_MEMBER;


SELECT * FROM react_member;

SELECT * FROM REACT_MEMBER WHERE  MEM_ID = 'java';




DROP TABLE board;




CREATE TABLE BOARD(
	BOARD_NUM INT PRIMARY KEY AUTO_INCREMENT
	,TITLE VARCHAR(30) NOT NULL
	,WRITER VARCHAR(30) NOT NULL
	,CONTENT VARCHAR(30)
	,READ_CNT INT DEFAULT 0
	,REG_DATE DATETIME DEFAULT SYSDATE()

);

SELECT * FROM board;

INSERT INTO BOARD VALUES(3,'제목33','작성자144','글내용11','2',NOW());


COMMIT;


CREATE TABLE BOARD_REPLY(
	REPLY_NUM INT PRIMARY KEY AUTO_INCREMENT
	,CONTENT VARCHAR(30)
	,WRITER VARCHAR(30) NOT NULL
	,REG_DATE DATETIME DEFAULT SYSDATE()
	,BOARD_NUM INT NOT NULL  REFERENCES board(BOARD_NUM) 
);

DROP TABLE BOARD_REPLY;


INSERT INTO BOARD_REPLY VALUES(2, 'AA','BB',NOW(), 4);

COMMIT;

SELECT * FROM board_reply;


SELECT * FROM board WHERE TITLE LIKE '%제목%' ; 

SELECT * FROM board WHERE WRITER LIKE '%java%' ; 












































